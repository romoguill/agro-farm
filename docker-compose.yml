services:
  postgres:
    image: postgres:latest
    container_name: agro-farm
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: postgres
    volumes:
      - ./pgdata:/var/lib/postgresql
